# Cloud Gaming Users Database (`cloudgame-db-users`)

Git-backed schema and data repository for user accounts, profiles, saved game states, and cloud gaming preferences.

## Structure
- `data/users.json`: Registered user accounts and save data
- `schema/user_schema.json`: JSON Schema for user profiles
- `scripts/sync_user.py`: Profile synchronization script
